---
import { pickLocale } from '../cms/pickLocale'

export const prerender = false

const locale = pickLocale(Astro.url.pathname, Astro.request.headers.get('accept-language'))
return Astro.redirect(`/${locale}`, 302)
---
